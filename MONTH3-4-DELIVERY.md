# تسليم الشهرين الثالث والرابع — NetXpert AI

## المنجز

أضيف **محرر شبكة Dijkstra تفاعلي** يسمح بإنشاء عقد جديدة وإضافة وصلات بأوزان موجبة، ثم حساب أقصر مسار مع إبراز المسار الناتج بصرياً. يعتمد الحساب على منطق Dijkstra النقي الموجود في `logic/dijkstra-logic.js`، ولذلك يبقى قابلاً للاختبار ولا يعتمد على DOM.

أضيف **كاشف شذوذ شبكي تعليمي قابل للتفسير** في `logic/anomaly-logic.js`. يحسب درجة من 0 إلى 100 من مؤشرات واضحة مثل عنوان مصدر عام، منفذ غير صحيح، انفجار الاتصالات، ومحاولات الدخول الفاشلة. لا يرسل بيانات العينة إلى أي خدمة خارجية، وتظهر للمستخدم أسباب القرار، مع تنبيه صريح بأنه نموذج تعليمي وليس IDS/IPS إنتاجياً.

أضيف **نظام نقاط وشارات** يحفظ بيانات التقدم المجهولة في `localStorage` فقط. تشمل النقاط أدوات Subnetting وIPv6 وVLSM والمقارنة وDijkstra وكشف الشذوذ، وتظهر أربعة مستويات من الشارات.

أضيفت **لوحة إحصائيات وتقدم** تعرض عدد النشاطات، عدد التنبيهات، معدل الشذوذ، وتوزيع الاستخدام حسب الأداة. صممت هذه النسخة لتعمل دون قاعدة بيانات أو نقل بيانات شخصية؛ ويمكن لاحقاً تحويلها إلى لوحة إدارة مركزية بعد تحديد حسابات المشرف وقواعد الوصول في Firebase.

## الاختبارات

تمت إضافة اختبارات Jest للمنطق الجديد في:

- `tests/anomaly-logic.test.js`
- `tests/gamification-logic.test.js`
- `tests/dashboard-logic.test.js`

النتيجة الموثقة: **10 مجموعات اختبار ناجحة، 110 اختباراً ناجحاً، 0 فشل**.

## اختبار المستخدمين الحقيقيين المقترح

يستخدم الباحث خمس مهام قصيرة مع 5–8 طلاب شبكات: إنشاء شبكة من أربع عقد وحساب مسار، تعديل وزن وصلة وملاحظة تغير المسار، تحليل عينتين طبيعية وشاذة، الوصول إلى لوحة التقدم، ثم شرح معنى درجة الشذوذ. يسجل الباحث زمن إنجاز المهمة، نسبة الإتمام، الأخطاء، ومقياس سهولة من 1 إلى 5، دون حفظ البريد أو أي معرف شخصي. بعد الجلسة تجمع الملاحظات في جدول مجهول، ثم ترتب الإصلاحات حسب شدة أثرها وقابلية تكرارها.

## التشغيل

```bash
npm install
npm test -- --runInBand
```

يفتح التطبيق مباشرة عبر `index.html` أو عبر خادم ملفات ثابت. لا توضع مفاتيح أو أسرار جديدة في الواجهة.

## ملاحظة هندسية

لوحة الإحصائيات الحالية محلية ومناسبة للنموذج الأكاديمي والعرض. لتصبح لوحة Admin مركزية متعددة المستخدمين، يجب في المرحلة التالية إضافة مجموعات Firestore وقواعد Security Rules وتحديد حسابات المشرف قبل نشر بيانات الاستخدام خارج المتصفح.

## إصلاح بنية الواجهة

تم نقل أقسام محرر Dijkstra وكشف الشذوذ ولوحة الإحصائيات إلى داخل `main.main-content` قبل إغلاق الحاويات، مع إضافة إغلاق `</main>` صريح؛ وبذلك أصبحت الأقسام الجديدة ضمن نفس مساحة العمل وتعمل مع التنقل الجانبي بشكل صحيح.

# الاختبارات الآلية (الشهر 1 - المهمة 2)

## شو تغيّر بالمشروع

الـ 15 Test Case اليدوية صارت اختبارات Jest حقيقية بتشتغل أوتوماتيكياً.
عشان هيك، استخرجنا "المنطق الحسابي الصرف" (بدون أي تعامل مع DOM) من كل
أداة لملفات منفصلة بمجلد `logic/`، وربطناها بالكود الأصلي (بدل ما تكون
مكررة)، وكتبنا اختبارات إلها بمجلد `tests/`.

### هيكلية جديدة:
```
logic/
  subnet-logic.js       <- منطق حاسبة الـ Subnetting (كان جوا app.js)
  dijkstra-logic.js     <- منطق خوارزمية Dijkstra (كان جوا algorithm-simulator.js)
  packet-filter-logic.js<- منطق فلترة الحزم (كان جوا packet-analyzer.js)
  terminal-logic.js     <- مخرجات أوامر التيرمنال (كان جوا terminal-simulator.js)

tests/
  subnet-logic.test.js
  dijkstra-logic.test.js
  packet-filter-logic.test.js
  terminal-logic.test.js
```

**مهم:** الملفات الأصلية (`app.js`, `algorithm-simulator.js`, ...) صارت
تستورد (`import`) هاد الدوال بدل ما تكررها، فسلوك الموقع نفسه ما تغيّر
أبداً — بس صار المنطق قابل للاختبار بمعزل عن الواجهة.

## طريقة التشغيل

### 1. ثبّت الاعتماديات (مرة وحدة)
افتح Terminal جوا مجلد المشروع ونفذ:
```bash
npm install
```

### 2. شغّل الاختبارات
```bash
npm test
```
رح يطلعلك تقرير فيه كل الاختبارات (✓ أخضر = ناجح) — حالياً عندنا 45 اختبار
موزعين على الأربع أدوات الأساسية.

### أوامر إضافية مفيدة
```bash
npm run test:watch     # يعيد التشغيل تلقائياً كل ما تعدل كود
npm run test:coverage  # يطلع تقرير نسبة تغطية الكود بالاختبارات
```

## ليش هاد مهم للمناقشة
- بيثبت إن الميزات الأساسية (Subnetting, Dijkstra, Packet Filter, Terminal)
  شغالة صح بشكل موثق وقابل لإعادة التحقق، مو بس "جربتها يدوياً ومشيت".
  بمجرد ما تضيف ميزة جديدة أو تعدل كود قديم، بتشغل `npm test` وتتأكد إنك
  ما كسرت شي موجود (Regression Testing).
